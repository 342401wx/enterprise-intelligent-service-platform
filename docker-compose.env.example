# Optional host data directory. Keep it outside GitHub.
DOCKER_DATA_DIR=./runtime-data

# Browser port exposed by the frontend container.
APP_PORT=5173

# Test account password. Change it for a real deployment.
PLATFORM_TEST_PASSWORD=12345678